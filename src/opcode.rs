#[derive(Debug)]
pub enum Opcode {
  Push (u32),
  Pop,
  Load (u32),
  StoreGlobal (String),
  LoadGlobal (String),
  Call (u32),
  Jmp (u32),
  PopJmpIfFalse (u32),
  Add,
  Subtract,
  Multiply,
  Divide,
  Mod,
  Equal,
  LessThan,
  GreaterThan,
  Negate,
}